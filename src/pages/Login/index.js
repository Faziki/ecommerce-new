import React from 'react';
import './styles.scss'

//component imports
import SignIn from '../../components/SignIn';

const Login = (props) => {
    return (
        <div>
            <SignIn />
        </div>
    );
};

export default Login;